[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dr-plugin-search-wiki-crawler"
version = "0.1.0"
description = "BFS wiki crawler search plugin (Lexicanum/TVTropes/Fandom) for the deepresearch plugin-host."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Proprietary" }
dependencies = [
    "dr-plugin-sdk",
    "grpcio>=1.65",
    "protobuf>=5,<8",
    "httpx>=0.27",
    "beautifulsoup4>=4.12",
    "lxml>=5.0",
    "markdownify>=0.11",
]

[project.optional-dependencies]
test = ["pytest>=8", "pytest-asyncio>=0.23"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["dr_plugin_search_wiki_crawler*"]

[tool.setuptools.package-data]
"dr_plugin_search_wiki_crawler" = ["../plugin.json"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.uv.sources]
dr-plugin-sdk = { path = "../_sdk" }
