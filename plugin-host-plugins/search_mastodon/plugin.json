{
  "id": "search_mastodon",
  "name": "Mastodon",
  "description": "Mastodon status search via a configurable instance (requires per-instance bearer token).",
  "version": "0.1.0",
  "category": "search",
  "capabilities": ["search.query"],
  "command": ["/opt/dr-plugin-venv/bin/python", "-m", "dr_plugin_search_mastodon"],
  "transport": "grpc-unix",
  "config_schema": {
    "type": "object",
    "properties": {
      "instance": {
        "type": "string",
        "description": "Mastodon instance hostname (no scheme), e.g. mastodon.social.",
        "default": "mastodon.social"
      },
      "access_token": {
        "type": "string",
        "description": "OAuth bearer token granted by the configured instance."
      }
    },
    "required": ["access_token"]
  },
  "args_schema": {
    "type": "object",
    "properties": {}
  },
  "supervisor": {
    "idle_timeout_seconds": 600,
    "max_restart_per_min": 3
  }
}
