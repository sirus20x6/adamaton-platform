# Vendored generated stubs. _pb.py prepends this dir to sys.path so the
# generated files' `from dr.plugin.v1 import ...` lines resolve here
# instead of leaking into a top-level `dr` namespace package.
